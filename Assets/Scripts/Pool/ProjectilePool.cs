using UnityEngine;

public class ProjectilePool : ObjectPool
{
    public static ProjectilePool Instance { get; private set; }

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
